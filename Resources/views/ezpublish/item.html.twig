{{ render(
    controller(
        'ez_content:viewAction', {
            'content': content,
            'location': location,
            'viewType': 'ngcb_preview',
            'layout': false,
        }
    )
) }}
