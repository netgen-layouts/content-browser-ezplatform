{{ render(
    controller(
        'ez_content:viewAction', {
            'contentId': item.location.contentInfo.id,
            'locationId': item.id,
            'viewType': 'line'
        }
    )
) }}
