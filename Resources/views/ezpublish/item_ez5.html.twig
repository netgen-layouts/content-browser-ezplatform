{{ render(
    controller(
        'ez_content:viewLocation', {
            'content': content,
            'location': location,
            'viewType': 'ngcb_preview',
            'layout': false,
        }
    )
) }}
