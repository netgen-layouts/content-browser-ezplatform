{% if content.fields.image is defined and not ez_is_field_empty( content, 'image' ) %}
    {% set image_alias = ez_image_alias( ez_field( content, 'image' ), content.versionInfo, 'netgen_content_browser' ) %}

    <img src="{{ image_alias.uri }}" />
{% endif %}
