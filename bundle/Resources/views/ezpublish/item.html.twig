{{ render(
    controller(
        'ez_content:viewAction', {
            'contentId': content.contentInfo.id,
            'locationId': location.id,
            'viewType': 'ngcb_preview',
            'layout': false,
        }
    )
) }}
