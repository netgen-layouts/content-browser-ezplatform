{{ render(
    controller(
        'ez_content:viewLocation', {
            'content': item.content,
            'location': item.location,
            'viewType': 'ngcb_preview',
            'layout': false,
        }
    )
) }}
